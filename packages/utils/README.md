# @pgpm-testing/utils

> **Testing Fixture:** This repository is a fork of pgpm-modules used for testing purposes. All modules are published under the `@pgpm-testing` scope instead of `@pgpm`. Use these modules for integration testing, CI pipelines, and development fixtures.
